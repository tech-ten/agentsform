# StudyMate Environment Variables
# Copy this file to .env and fill in the values
#
# IMPORTANT: These variables are used during CDK deployment.
# Run: source .env && npx cdk deploy --all
#
# After deployment, you can also update Lambda env vars directly:
# aws lambda update-function-configuration --function-name <name> --environment "Variables={...}"

# ===================
# AWS Configuration
# ===================
# AWS credentials should be configured via AWS CLI or environment
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=ap-southeast-2

# ===================
# Groq AI API
# ===================
# Get your API key from: https://console.groq.com/keys
# Used by: agentsform-aihandler (AI tutor and explanations)
# NOT used by: adminhandler, paymenthandler, questionhandler, etc.
GROQ_API_KEY=gsk_your_groq_api_key_here

# ===================
# Admin Dashboard
# ===================
# Admin API key for accessing /admin endpoints
# This is separate from parent authentication - for AgentsForm personnel only
# Access the admin dashboard at: https://tutor.agentsform.ai/admin
ADMIN_API_KEY=studymate-admin-your-secure-key-here

# ===================
# Stripe Payments
# ===================
# Get these from https://dashboard.stripe.com
# Used by: studymate-paymenthandler, agentsform-adminhandler (for payment data)

# Secret key (starts with sk_live_ or sk_test_)
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key

# Webhook signing secret (starts with whsec_)
# Create webhook at: Dashboard > Webhooks > Add destination
# Endpoint URL: https://yhn9tli08d.execute-api.ap-southeast-2.amazonaws.com/payments/webhook
# Events: checkout.session.completed, customer.subscription.updated, customer.subscription.deleted
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret

# Price IDs from your Stripe product catalog
# Create products at: Dashboard > Product catalog > Add product
STRIPE_PRICE_SCHOLAR=price_your_scholar_price_id
STRIPE_PRICE_ACHIEVER=price_your_achiever_price_id

# ===================
# Frontend URL
# ===================
FRONTEND_URL=https://tutor.agentsform.ai

# ===================
# Lambda Functions Reference
# ===================
# agentsform-aihandler      - AI tutor chat & explanations (uses GROQ_API_KEY)
# agentsform-questionhandler - Question generation (uses GROQ_API_KEY for AI-generated questions)
# agentsform-adminhandler   - Admin dashboard stats (uses STRIPE_*, ADMIN_API_KEY)
# agentsform-childhandler   - Child profile management (no external APIs)
# agentsform-progresshandler - Progress tracking (no external APIs)
# agentsform-gamificationhandler - Badges & rewards (no external APIs)
# agentsform-benchmarkhandler - Benchmark tests (no external APIs)
# studymate-paymenthandler  - Stripe payments (uses STRIPE_* keys)
